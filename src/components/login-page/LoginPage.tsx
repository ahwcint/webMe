'use client';

import React from 'react';
import { Button } from '../ui/button';

export default function LoginPage() {
  return (
    <div className={'p-3 bg-red'}>
      <h1 className={'text-3xl font-bold underline'}>12345</h1>
      <Button variant={'outline'}>Button</Button>
    </div>
  );
}
